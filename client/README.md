# OCMS Client

React UI for the Online Complaint Management System.

## Run (dev)
- From repo root: `npm run dev`

## Notes
- The dev server proxies `/api` to `http://localhost:5000` (configured in `vite.config.js`).
- Users can register and submit complaints.
- Admins log in using the seeded admin account (see root README).

